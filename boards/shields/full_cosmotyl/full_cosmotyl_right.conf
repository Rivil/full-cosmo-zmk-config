CONFIG_ZMK_POINTING=y

# Peripheral-side battery reporting. BLE + BAS is usually implied, but set it
# explicitly so the left side has something to read over GATT for the R %.
CONFIG_ZMK_BATTERY_REPORTING=y
CONFIG_BT_BAS=y
