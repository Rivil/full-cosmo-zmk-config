# NOTE: This file is NOT picked up by the build because the split shield names
# (full_cosmotyl_left / full_cosmotyl_right) don't match this file's basename.
# Active firmware configs live in build.yaml under `cmake-args` instead.
