# board: [ "nice_nano" ]
# shield: [ "corne_left", "corne_right" ]
# NOTE: Studio temporarily disabled to diagnose mouse HID issue.
# To re-enable: add `snippet: studio-rpc-usb-uart` and `cmake-args: -DCONFIG_ZMK_STUDIO=y` back to both entries.
 include:
   - board: nice_nano_v2
     shield: full_cosmotyl_left nice_view_adapter nice_view
   - board: nice_nano_v2
     shield: full_cosmotyl_right nice_view_adapter nice_view
